{
  "name": "@xpertia/database",
  "version": "1.0.0",
  "description": "Esquema y configuración de SurrealDB para Xpertia Plataforma",
  "main": "config.ts",
  "type": "module",
  "scripts": {
    "init": "./init-schema.sh",
    "init:dev": "SURREAL_URL=http://localhost:8000 ./init-schema.sh",
    "init:test": "NAMESPACE=xpertia_test DATABASE=plataforma_test ./init-schema.sh",
    "reset:dev": "surreal sql --endpoint http://localhost:8000 --username root --password root --namespace xpertia --database plataforma 'REMOVE DATABASE plataforma;' && npm run init:dev",
    "clean": "tsx clean.ts",
    "clean:dev": "NODE_ENV=development tsx clean.ts",
    "fresh": "npm run clean:dev && npm run init:dev",
    "seed": "node --loader ts-node/esm seed.ts",
    "seed:dev": "SURREAL_URL=http://localhost:8000 node --loader ts-node/esm seed.ts",
    "query": "surreal sql --endpoint http://localhost:8000 --username root --password root --namespace xpertia --database plataforma",
    "import": "surreal import --endpoint http://localhost:8000 --username root --password root --namespace xpertia --database plataforma",
    "export": "surreal export --endpoint http://localhost:8000 --username root --password root --namespace xpertia --database plataforma",
    "reset": "./reset-db.sh",
    "reset:confirm": "./reset-db.sh --confirm",
    "migrate": "tsx reset-and-migrate.ts",
    "migrate:confirm": "tsx reset-and-migrate.ts --confirm"
  },
  "keywords": [
    "surrealdb",
    "database",
    "schema",
    "xpertia"
  ],
  "author": "Xpertia Team",
  "license": "MIT",
  "dependencies": {
    "surrealdb.js": "^0.11.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "typescript": "^5.3.3"
  },
  "files": [
    "schema/**/*.surql",
    "config.ts",
    "init-schema.sh",
    "queries-ejemplos.surql",
    "README.md"
  ]
}
